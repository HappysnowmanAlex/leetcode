#include <vector>

using namespace std;

class Solution {
public:
  vector<int> findDiagonalOrder(vector<vector<int>> &mat) {
    vector<int> nums;
    int row = 0, col = 0;
    int const MAX_ROW = nums.size(), MAX_COL = nums[0].size() - 1;
    bool up = true;
    while (row < mat.size() && col < mat[0].size()) {
    }
    return nums;
  }
};
